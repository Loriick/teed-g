import { styled } from 'styled-components';

export const MainWrapper = styled.main`
  grid-area: main;
  padding: 4rem 2.4rem;
  max-width: 1024px;
  margin: 0 auto;
  .media-container {
    display: flex;
  }
`;
